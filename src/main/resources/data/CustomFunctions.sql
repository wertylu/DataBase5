USE `steam_db`;

DROP FUNCTION IF EXISTS GetAverageAge;
DELIMITER //
CREATE FUNCTION GetAverageAge()
    RETURNS DECIMAL(8, 2)
    DETERMINISTIC
BEGIN
    RETURN (SELECT AVG(age) FROM `user`);
END //
DELIMITER ;